services:

  db:
    image: postgres:latest
    container_name: hnt-db
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - /python/data:/var/lib/postgresql/data
      - /python/data/store.sql:/docker-entrypoint-initdb.d/init_data.sql:ro
    networks:
      - hnt-network

  web:
    image: django-docker
    container_name: hnt-web
    build: .
    command: gunicorn --bind 0.0.0.0:8000 --workers 3 store.wsgi:application
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    networks:
      - hnt-network

  nginx:
    image: nginx:latest
    container_name: hnt-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./static:/app/static
      - ./media:/app/media
    depends_on:
      - web
    networks:
      - hnt-network

#  certbot:
#    image: certbot/certbot
#    container_name: certbot
#    volumes:
#      - ./certbot/conf:/etc/letsencrypt
#      - ./certbot/www:/var/www/certbot
#    entrypoint: >
#      sh -c "certbot certonly --webroot --webroot-path=/var/www/certbot
#      --email nthieu141204@gmail.com --agree-tos --no-eff-email
#      -d hieunamson.id.vn"
