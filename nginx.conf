server {
    listen 80;
    server_name hieunamson.id.vn;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        alias /app/static/;
    }
    location / {
        proxy_pass http://web:8000;
    }
}
